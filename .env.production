# Public client-side Supabase config for production builds.
# Safe to commit: the publishable (anon) key is designed for browser use and is
# already shipped in the client bundle; data is protected by RLS + restricted
# grants. Kept here because the CI `vercel build` step does not reliably inject
# Vercel project env vars into the Vite build.
VITE_SUPABASE_URL=https://wbxyyvvuqrhqtuywfeto.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_cx-TCDpCHAp1PrH2owzGkw_AY7KQHtK
